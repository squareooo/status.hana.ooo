extend type Mutation {
  "블록 생성"
  createBlock(input: CreateBlockInput!): CreateBlock!
}

input CreateBlockInput {
  testId: ID!
  markdown: String!
}

type CreateBlock {
  id: ID!
  testId: ID!
  userId: ID!
  markdown: String!
}
